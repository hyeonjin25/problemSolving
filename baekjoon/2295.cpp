#include<bits/stdc++.h>
using namespace std;

int b_search(int a){
  int st=0,en=n-1;
  while(st<=en){
    int mid = (st+en)/2;
    if(mid>target) en=mid-1;
    else if(mid<target) en=
  }

int main(){
  ios::sync_with_stdio(0); cin.tie(0);

  for(int i=0; i<)
}